import 'package:flutter/material.dart';

/// Central color palette for the FixNow app, matching the approved
/// persona/UI theme (warm orange primary on a clean off-white surface).
class AppColors {
  AppColors._();

  static const Color primary = Color(0xFFFF6B35);
  static const Color primaryDark = Color(0xFFE85A2A);
  static const Color primaryLight = Color(0xFFFFE4D6);

  static const Color textDark = Color(0xFF2D3142);
  static const Color textMuted = Color(0xFF6B7280);

  static const Color background = Color(0xFFFAF8F6);
  static const Color surface = Colors.white;
  static const Color border = Color(0xFFE7E3E0);

  static const Color success = Color(0xFF2E9E5B);
  static const Color error = Color(0xFFE0483E);
  static const Color rating = Color(0xFFFFB020);
}
